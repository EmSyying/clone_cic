import 'package:flutter/material.dart';

BoxDecoration containerCiCPartern = BoxDecoration(
    boxShadow: const [
      BoxShadow(offset: Offset(1.0, 0.0), color: Colors.black12, blurRadius: 6)
    ],
    borderRadius: BorderRadius.circular(10),
    border: Border.all(color: Colors.white),
    color: Colors.white);

const blurRadius = 6.0;
const color = Colors.black12;
const borderRaduis = 10.0;
const offSet = Offset(1.0, 0.0);
const padding = 20.0;
